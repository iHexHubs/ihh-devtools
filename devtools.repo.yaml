schema_version: 1
registries:
  # Defaults permitidos. Ver ADR 0002-services-source-of-truth.md.
  # - build  : ".devtools" + "/config/apps.yaml" (placeholder, B-5 lo definira)
  # - deploy : "ecosystem/services.yaml" (default operativo; consumer puede sobreescribir)
  build: null
  deploy: ecosystem/services.yaml
paths:
  vendor_dir: .devtools
config:
  profile_file: .git-acprc
