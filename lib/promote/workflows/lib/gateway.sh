#!/usr/bin/env bash
# shellcheck shell=bash

cmd_gateway() {
    die_rc "$RC_USAGE" "Subcomando 'gateway' aún no migrado. Usa 'git promote local' para flujo completo mientras se completa la Fase B."
}
