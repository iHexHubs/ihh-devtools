surface: print-env
type: export-script
required_exports:
  - name: DEVBOX_ENV_NAME
    expected: IHH
  - name: DEVBOX_PROJECT_ROOT
    expected: <repo_root>
dynamic_fields:
  - DEVBOX_PROJECT_ROOT
notes:
  - Additional exports are allowed as long as the protected exports remain present.

