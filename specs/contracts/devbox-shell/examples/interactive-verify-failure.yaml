scenario_id: interactive-verify-failure
boundary: devbox shell
mode: interactive-strict
preconditions:
  - .devtools/.setup_completed exists
  - tty available
  - DEVTOOLS_SKIP_WIZARD is not 1
  - setup-wizard verify-only fails
inputs:
  command: devbox shell
expected_observables:
  ready_state: contextualized-session-disabled
  messages:
    - verification failure is visible
  invariants:
    - no ready path is claimed after failed verification
coverage_note: currently validated structurally, not as full end-to-end tty session
