scenario_id: print-env-surface
consumer: non_interactive_inspector
command: devbox shell --print-env
expected_contractual_observables:
  - exit status is zero
  - output exports `DEVBOX_ENV_NAME=IHH`
  - output exports `DEVBOX_PROJECT_ROOT` for this repo
notes:
  - this example validates the limited inspection surface
  - it must not be interpreted as proof of a full interactive session
