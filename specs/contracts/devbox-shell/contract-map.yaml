flow_id: devbox-shell
adoption_mode: hybrid
canonical_contract:
  type: cli-shell-observable-contract
  file: specs/contracts/devbox-shell/assertions/cli-contract.yaml
  executable: false
executable_contract:
  type: bats-shell-contract-harness
  file: tests/contracts/devbox-shell/devbox-shell-contract.bats
interface_contract: specs/contracts/devbox-shell/interface-contract.yaml
consumer_needs: specs/contracts/devbox-shell/consumer-needs.yaml
compatibility_rules: specs/contracts/devbox-shell/compatibility-rules.yaml
examples_dir: specs/contracts/devbox-shell/examples
schemas_dir: specs/contracts/devbox-shell/schemas
assertions_dir: specs/contracts/devbox-shell/assertions
tests_contracts_dir: tests/contracts/devbox-shell
ci_contract_checks: .ci/contract-checks.yaml
primary_validation_tool: bats
specmatic_scope:
  enabled: false
  role: no_aplica
  contract: none
  validates: []
  reason: frontera principal CLI/shell local sin dialecto directo honesto en Specmatic
coverage_scope:
  complete: []
  partial:
    - gate observable de la variante estricta
    - export surface de devbox shell --print-env
    - trazabilidad del consumer lib/promote/workflows/common.sh
  out_of_scope:
    - sesion interactiva completa con TTY y credenciales reales
    - internals de Devbox/Nix fuera de la frontera observable
